[phases.setup]
nixPkgs = ["nodejs_18", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

